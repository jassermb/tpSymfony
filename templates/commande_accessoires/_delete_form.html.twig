<form method="post" action="{{ path('app_commande_accessoires_delete', {'id': commande_accessoire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_accessoire.id) }}">
 <cneter >   <button class="btn">Delete</button> </center>
</form>
